
" First set the font, note that this is different on macvim versus gvim
if has("gui_gtk2")
    set guifont=Monaco\ 12
elseif has("gui_macvim")
    set guifont=Monaco:h14
end

" Set default width for NERDTree panel
let g:NERDTreeWinSize = 45

